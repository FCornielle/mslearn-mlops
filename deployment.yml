$schema: https://azuremlschemas.azureedge.net/latest/managedOnlineDeployment.schema.json

name: diabetes-deployment
endpoint_name: diabetes-endpoint-xyz3

# reference your registered MLflow model by name:version
model: azureml:diabetes_prod_model:1

# tell Azure ML which inference image to use
environment: azureml:AzureML-mlflow-1.32-ubuntu18.04-py37-cpu@latest

instance_type: Standard_D2as_v4
instance_count: 1